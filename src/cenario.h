#pragma once
#include <SDL2/SDL.h>
#include <stdio.h>
#include "engine/scenes/SimpleScene.h"

class Cenario: public SimpleScene{
private:
    /* ATRIBUTOS PRIVADOS */
    /* MÉTODOS PRIVADOS */
public:
    Cenario();
    ~Cenario();
    /* ATRIBUTOS PÚBLICOS */
    /* MÉTODOS PÚBLICOS */
};