#pragma once
#include <SDL2/SDL.h>
#include <stdio.h>

class Cenario(){
private:
    /* ATRIBUTOS PRIVADOS */
    /* MÉTODOS PRIVADOS */
public:
    /* ATRIBUTOS PÚBLICOS */
    /* MÉTODOS PÚBLICOS */
}